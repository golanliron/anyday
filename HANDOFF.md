# AnyDay — מסמך מסירה למיטל

עודכן: אוגוסט 2026

## החזון בשורה אחת
**AnyDay הופך בורד Monday של עמותה למערכת ניהול חכמה** — צופה, מבין, ומנהל.
לא רק מציג נתונים, אלא מאפשר לעדכן אותם (והשינוי חוזר ל-Monday), מזהה תובנות
לבד, ומדבר בשפה טבעית. **גנרי לחלוטין** — עובד לכל עמותה, לא רק לבורד ספציפי.

## מה כבר בנוי ועובד (מאומת מול בורד אמיתי)

| תחום | מצב | קובץ מרכזי |
|------|-----|-----------|
| חיבור ל-Monday האמיתי | ✅ עובד (50 בורדים, 2078 פריטים) | `lib/monday-server.ts` |
| בחירת בורדים (עד 2) + סרגל ימני | ✅ | `app/app/page.tsx` (BoardRail) |
| דשבורד גרפים צבעוני + KPIs **נגזרים** | ✅ | `api/dashboard` + `lib/board-intelligence.ts` |
| drill-down: לחיצה על גרף → שמות | ✅ | `page.tsx` (ChartBody) |
| תובנות "שמתי לב ש..." מנוסחות | ✅ (מאומת: "רמת השכלה ריקה 74%") | `api/insights` |
| משתתפים — כרטיסים + רשימה יפים | ✅ | `page.tsx` (People) |
| **ניהול מלא: עריכה/הוספה/מחיקה/ייבוא CSV** | ✅ מאומת round-trip | `api/record` |
| עדכון סטטוס דרך הצ'אט + "מה ישתנה?" | ✅ | `api/ask` + `api/action` |
| צ'אט חופשי (AI) על הבורד | ✅ (עם ANTHROPIC_API_KEY) | `api/ask` |
| שפה גנרית — terminology נגזר מהבורד | ✅ (בוגרים/מוטבים/קשישים...) | `board-intelligence.ts` |
| מפת האימפקט (קונסטלציה) | 🟡 בנוי בקוד, לא בשימוש (יצא צפוף) | `api/constellation` + ImpactMap |

## עקרון הזהב — לשמור עליו בכל פיצ'ר חדש
**הכל נגזר מהבורד, אפס מילים מקודדות.** לדוגמה — אסור לכתוב "משתתפים"
בקוד; קוראים ל-`terminology(board)` שמחזיר "בוגרים"/"מוטבים"/"קשישים" לפי
הבורד. הזיהוי הוא לפי **טיפוס העמודה** (status/date/people/numbers), לא לפי
שם. זה מה שמאפשר שאותו קוד ישרת עמותת נפגעי-מלחמה בדיוק כמו עמותת בוגרים.

## פיצ'רים הבאים בתור (מדוברים, טרם נבנו)
1. **📋 שאלונים מוגנים** — לשלוח שאלון (נגזר מעמודות הבורד) למוטב, התשובות
   חוזרות אוטומטית לבורד. Monday תומך ב-`create_form` — ראו MCP.
2. **⚡ אוטומציות אמיתיות** — "כשמוטב עובר ל'סיים' → שלח התראה". לא סתם —
   לחשוב מה *באמת* שימושי לעמותה.
3. **🖼 תמונת דשבורד** — כפתור "ייצא תמונה" → PNG אמיתי להורדה/שיתוף.
4. **🕸️ מסע המשתתף** — ציר זמן שמספר סיפור (הצטרף → מיפוי → תוכנית → השלים).
   קוד ראשוני קיים ב-DotProfile.
5. **מפת אימפקט מתוקנת** — הקונסטלציה יפה אבל צריך פיזור טוב יותר (הבעיה:
   בורד עם ערך דומיננטי "אחר" → נקודות דחוסות). ראו `api/constellation`.

## הרצה — ראו `README.md`
המינימום: `MONDAY_PERSONAL_TOKEN` ב-`.env.local`, `npm install`, `npm run dev`,
פתחו `/app`.

## בטיחות — חשוב
- **סודות לא נכנסים ל-git** (`.env*` ב-gitignore). כל אחת מכניסה טוקן משלה.
- כתיבה ל-Monday: עריכה/הוספה מיידית, **מחיקה תמיד עם אישור**.
- לפני כל קומיט: `npx tsc --noEmit` + `npm run build` חייבים לעבור נקי.

## שאלות? הקוד מתועד — התחילו מ-`board-intelligence.ts` (הלב), ואז `api/ask`
(מוח הצ'אט). בהצלחה 🟣
